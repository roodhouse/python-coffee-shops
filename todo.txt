1. frontend
    dashboard view
        update post view

2. backend setup and connection
    - create tables and populate
        - user (done)
        - venues (expand on model and create frontend connection)
            -- route
            -- frontend fetch route
            -- return only venues for specific location
            -- change count to number of venues in current location
        - reviews
        - comments
    - connect forms (answers)
    - create logic for aggregating results

3. API's needed:
    - google maps